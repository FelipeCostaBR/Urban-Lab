import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    input: {
        borderColor: '#000',
        borderWidth: 1,
        borderRadius: 20,
        
        width: '100%',
        height: 40,

        paddingTop: 5,
        paddingBottom: 5,
        paddingLeft: 15,
        paddingRight: 15,

        marginBottom: 15
    }
})

export default styles